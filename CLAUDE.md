# ganpereh-site

אתר שיווקי סטטי של גן פרא (גינון ואדריכלות נוף), עברית RTL, מוגש
מ-GitHub Pages בדומיין `ganpereh.co.il`.

**מתכנן הארוחות עבר למאגר משלו** (28/07/2026): `yarin-debug/meal-planner`.
הוא ישב כאן תחת `app/` ושאל ארבעה קבצי פונט מ-`fonts/`. שום דבר אחר לא
היה משותף — לא CSS, לא JS, ולא קישור מאף עמוד. ההצמדה גבתה מחיר בכל
פריסה: שינוי באפליקציה פרטית נכנס לאותו `main` של אתר לקוחות. ההפרדה
מלאה, כולל עותק פונטים משלו. **אין לשחזר את `app/` לכאן.**

`fonts/` נשאר כאן ובשימוש: `index.css` מגדיר ממנו Alef ו-TelAviv
Brutalist, ועמודי הפרויקטים טוענים מראש גם TelAviv Modernist.

---

## מבנה

HTML סטטי. **אין לערוך `<nav>` או `<footer>` בעמודים ישירות** — הם
מוזרקים מ-`partials/`:

```bash
python3 build.py            # הרצה יבשה, מדווחת מה ישתנה
python3 build.py --write    # מיישם
```

`build_projects.py` מייצר את גלריית הפרויקטים (`projects.html` + עמוד לכל
פרויקט) ממבנה `PROJECTS` שבתוכו. להוספת פרויקט: רשומה במבנה, תמונות
ל-`images/projects/<slug>/`, והרצת הסקריפט.

`index.html`, `5.html`, `quiz.html`, `landing-misradim.html` לא מקבלים את
התפריט המשותף.

## פרמוט

```bash
npx prettier@3 --write "**/*.{css,js}"
```

`.prettierignore` מחריג HTML בכוונה — ה-HTML של האתר השיווקי הוא סיכון
ויזואלי ונשאר ידני. `.prettierrc` קובע `printWidth: 100`; בלעדיו
prettier מדווח על קבצים תקינים כשבורים.

---

## Git

פיתוח על ענף ייעודי, לעולם לא ישירות על `main`.
הודעות קומיט בעברית, בגוף ההודעה מסבירים *למה* ולא רק *מה*.
**מיזוג ל-`main` הוא הפריסה.**

`.gitignore` מחריג מסמכים עסקיים רגישים (`הצעות מחיר/` — שמות, כתובות
ומחירים של לקוחות; `אוטומציה/`). המאגר ציבורי. לא להוציא משם דבר בלי
לבדוק מה נחשף.
