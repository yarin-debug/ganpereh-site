# ganpereh-site

שני פרויקטים באותו מאגר:

| | מה | איפה |
|---|---|---|
| **האתר** | אתר שיווקי סטטי של גן פרא (גינון ואדריכלות נוף), עברית RTL, מוגש מ-GitHub Pages | שורש המאגר |
| **מתכנן הארוחות** | אפליקציית ווב פרטית לשימוש אישי, `noindex` | `app/` |

השניים לא חולקים CSS או JS. הם חולקים רק את `fonts/`.

---

## איך מדברים כאן — חל על שני הפרויקטים

בעל המאגר אינו מתכנת. המטרה כפולה: גם לבנות טוב, וגם ללמוד תוך כדי.
לכן כל תשובה נמדדת גם בשאלה "האם היא מלמדת משהו שיישאר גם בפעם הבאה".

- **עברית פשוטה.** מונח מקצועי מקבל הסבר של משפט אחד בפעם הראשונה שהוא
  מופיע בסשן — "service worker (הרכיב שמחזיק עותק של האפליקציה במכשיר,
  כדי שתעבוד גם בלי אינטרנט)". מונחים חוזרים מרוכזים ב-`docs/glossary.md`,
  ומונח חדש שנכנס לשימוש קבוע — להוסיף לשם.
- **קודם העיקרון, אחר כך הפרטים.** לפתוח במשפט על *מה* השתנה ו*למה* זה
  עובד ככה, ורק אז לרדת לשמות קבצים. אם ההסבר נכון רק כאן ולא בשום
  פרויקט אחר — הוא צר מדי.
- **לא להעתיק פלט גולמי.** לוגים, שגיאות ודיפים ארוכים מסוכמים במשפט,
  ומצורפת רק השורה שקובעת.
- **שאלות סגורות עם המלצה.** לא "איך תרצה להמשיך?" אלא שתיים־שלוש
  אפשרויות קונקרטיות, מה ההבדל ביניהן *בפועל*, ואיזו מומלצת ולמה.
- **תמיד לסיים בהמשך.** כל תשובה נגמרת בצעד הבא המוצע ובהערכת גודל —
  "שינוי קטן, כמה דקות" מול "משימה שלמה, שווה סשן נפרד".
- **לפצל לענפים מקבילים.** שתי משימות שלא נוגעות באותם קבצים — ענף לכל
  אחת, ולהציע את זה מראש. משימות שכן מתנגשות רצות בטור, ומסבירים במה
  ההתנגשות.

---

## מתכנן הארוחות (`app/`) — אזור העבודה הפעיל

אפליקציית מובייל-תחילה, עברית RTL, לתכנון ארוחות במשק בית.
**קרא את `app/README.md` לפני שינוי בקוד** — שם יושבות ההחלטות
הארכיטקטוניות והנימוקים שלהן.

### הרצה ובדיקה

אין שלב build ואין תלויות. ES modules טהורים, נפתח משרת סטטי:

```bash
python3 -m http.server 8899 --bind 127.0.0.1
# האפליקציה:  http://127.0.0.1:8899/app/
# הבדיקות:    http://127.0.0.1:8899/app/dev/tests.html
```

`app/dev/tests.html` הוא הרנס היחיד — נפתח בדפדפן ומציג סיכום ירוק/אדום.
**324 בדיקות עוברות כרגע.** הן רצות מול מפתח localStorage ייעודי
(`gp_meals_test__do_not_use`) ולעולם לא נוגעות בנתוני הייצור.

לבדיקה אוטומטית מלאה יש Chromium מותקן. **אין להריץ `playwright install`.**
הנתיב המדויק הוא עם מספר הגרסה — `chromium` לבדו אינו קיים:

```js
chromium.launch({ executablePath: "/opt/pw-browsers/chromium-1194/chrome-linux/chrome" })
```

מתקינים `playwright` בתיקיית סקראץ' (לא במאגר — אין כאן `package.json`
ואין תלויות), ומריצים מול השרת הסטטי המקומי.

**מלכודת בכתיבת בדיקות דפדפן:** השכבות נערמות (בורר מנה ← בורר מצרך ←
טופס), וכל המסכים נשארים ב-DOM גם כשהם מוסתרים. סלקטור גלובלי כמו
`.dish-card` יתפוס אלמנטים משכבות אחרות וממסכים מוסתרים. יש למקד תמיד:
`page.locator(".sheet").last()` לשכבה העליונה, `#screen-list` למסך.

### פרמוט

```bash
npx prettier@3 --write "app/**/*.{css,js}"
```

`.prettierignore` מחריג HTML בכוונה — ה-HTML של האתר השיווקי הוא סיכון
ויזואלי ונשאר ידני.

### ⚠️ הגבול מול הדשבורד — לא לטשטש

מתכנן הארוחות מסתנכרן מול Supabase (`app/js/sync/`), והדשבורד
(`yarin-debug/ganpereh-dashboard`) מסתנכרן מול Supabase. **אלה חייבים
להיות שני פרויקטי Supabase נפרדים.**

הסיבה אינה סדר וניקיון. מדיניות ה-RLS של הדשבורד מתירה גישה לכל מי
שה-JWT שלו נושא כתובת מרשימה שכוללת את `yarin@ganpereh.co.il`, ופרויקט
Supabase חותם את כל האסימונים שלו באותו סוד. פרויקט משותף היה הופך
התחברות למתכנן הארוחות לאסימון תקף לקריאת הלידים, הצעות המחיר ופרטי
חשבון הבנק שב-CRM. סכמה נפרדת או קידומת לטבלאות **אינן** פותרות את זה
— הן חולקות את אותו סוד חתימה.

חמישה כללים שמחזיקים את ההפרדה:

| | הכלל |
|---|---|
| 1 | פרויקט Supabase נפרד. לא סכמה, לא קידומת |
| 2 | ה-SQL של הארוחות יושב ב-`app/supabase/` בלבד — לא נוגעים ב-`supabase/` של הדשבורד |
| 3 | אפס קוד משותף. אין ייבוא בין המאגרים לשום כיוון |
| 4 | האפליקציות לא קוראות זו לזו ב-API. השאלון באתר קורא לדשבורד, מתכנן הארוחות לא |
| 5 | `app/js/sync/config.js` לעולם לא מכיל את כתובת פרויקט הדשבורד. אם היא שם — זו תקלה |

בראש `app/supabase/schema.sql` יש בדיקת "אני בפרויקט הנכון?" שיש
להריץ לפני כל מיגרציה.

### ⚠️ מלכודת פריסה

`app/sw.js` מחזיק `VERSION` (כרגע `v16`). **כל פריסה שמשנה קובץ ברשימת
`SHELL` חייבת להעלות אותו**, אחרת משתמשים ימשיכו לקבל את הגרסה הישנה
מהמטמון. הוספת קובץ JS חדש? להוסיף אותו ל-`SHELL` *וגם* להעלות VERSION.

המטמון אטומי בכוונה (הכל או כלום) כדי למנוע "מטמון מעורב" — `index.html`
חדש לצד JS ישן.

**לאמת על התצוגה המקדימה של Netlify — לא רק מקומית.** לכל PR נבנית
תצוגה חיה בכתובת `https://deploy-preview-<מספר>--singular-biscotti-c916b6.netlify.app/app/`,
והיא הדבר הקרוב ביותר לייצור: היא תופסת נתיבים שבורים ורישום
service worker שנכשל — בדיוק מה שהשרת הסטטי המקומי לא מגלה, כי שם
הכל מוגש משורש נקי.

⚠️ **סביבת הסוכן חוסמת את המארח הזה** (הפרוקסי מחזיר 403 על CONNECT),
כלומר סשן של Claude לא יכול לבדוק את זה בעצמו ואסור לו לטעון שבדק.
זו בדיקה שנשארת לבן אדם עם דפדפן. שתי השאלות ששוות מבט:

1. בהתקנה נקייה (חלון פרטי) — מסך הפתיחה **כן** נפתח ומסיים כמו שצריך.
2. במכשיר שכבר מחזיק נתונים — מסך הפתיחה **לא** נפתח, ומשק הבית נשאר.

### עבודת עיצוב — חובה

כל שינוי ויזואלי עובר דרך הסקילים האלה, לא "לפי תחושה". זה כולל מסך
חדש, שינוי פריסה, טיפוגרפיה, צבע, מצבי ריק, ואנימציה.

| שלב | מה מפעילים | מתי |
|---|---|---|
| **כיוון** | סקיל `frontend-design` | *לפני* שכותבים שורת CSS ראשונה למסך חדש או לשינוי בשפה הוויזואלית |
| **מיקרו-קופי** | `/design:ux-copy` | כשכותבים מצבי ריק, הודעות שגיאה, ותוויות כפתורים |
| **ביקורת** | `/design:critique` | כשיש מסך עובד, לפני קומיט |
| **נגישות** | `/design:accessibility` | על כל מסך חדש, ובכל שינוי בפקדים |

`/design:*` הן פקודות של הפלאגאין **`design`** (מותקן ומופעל). אין
פלאגאין בשם `impeccable` — אם מחפשים אותו, זה השם הנכון.

**⚠️ הסקילים מעדנים בתוך השפה הקיימת, לא מתחילים מחדש.** לאפליקציה
כבר יש כיוון עיצובי מוכרע ומתועד ב-`app/README.md` תחת "עיצוב":
פלטת אריח המטבח (קובלט לזהות, חרס לפעולה), TelAviv Brutalist + Alef,
ופס השבוע כאלמנט החתום. `frontend-design` נועד לקבל החלטות *בתוך*
המערכת הזו. סבב שמחליף את הפלטה או את הפונטים בלי סיבה מפורשת הוא
רגרסיה, לא שדרוג.

**הכלל שאסור לשבור:** מילוי מלא שמור לפעולה *אחת* שצריך לעשות עכשיו.
מה שהושלם מקבל קו וגוון רך. הכלל הזה נולד מתלונה אמיתית — גרסה שציירה
"בישלנו" כמלבן כהה נפסלה כי *ביצוע* נקרא כמשקל.

תמיד לאמת ויזואלית לפני קומיט — יש Chromium מותקן, וצילום מסך ב-390×844
תופס פריסות שבורות שהקוד לבדו לא מגלה.

### מוסכמות כתיבה

- **הערות בעברית.** ההערה מסבירה *למה*, לא *מה*. הקוד כבר אומר מה.
- **ההערות שמתעדות החלטה נגד האינטואיציה — לא למחוק.** הן הסיבה שהחלטה
  נכונה לא מתהפכת בסיבוב הבא.
- אין תלויות חיצוניות ואין בקשות רשת חיצוניות. הפונטים מקומיים.
- כל מחרוזת שהמשתמש רואה היא בעברית.
- שינוי בלוגיקה טהורה (`normalize.js`, `plan.js`, `pantry.js`, `catalog.js`, `profiles.js`, `history.js`)
  מגיע עם בדיקה ב-`app/dev/tests.js`.

---

## האתר השיווקי (שורש)

HTML סטטי. **אין לערוך `<nav>` או `<footer>` בעמודים ישירות** — הם
מוזרקים מ-`partials/`:

```bash
python3 build.py            # הרצה יבשה, מדווחת מה ישתנה
python3 build.py --write    # מיישם
```

`build_projects.py` מייצר את גלריית הפרויקטים (`projects.html` + עמוד לכל
פרויקט) ממבנה `PROJECTS` שבתוכו. להוספת פרויקט: רשומה במבנה, תמונות
ל-`images/projects/<slug>/`, והרצת הסקריפט.

`index.html`, `5.html`, `quiz.html`, `landing-misradim.html` לא מקבלים את
התפריט המשותף.

---

## Git

פיתוח על ענף ייעודי, לעולם לא ישירות על `main`.
הודעות קומיט בעברית, בגוף ההודעה מסבירים *למה* ולא רק *מה*.
