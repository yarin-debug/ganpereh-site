# meal-planner

אפליקציית ווב פרטית לתכנון ארוחות במשק בית. מובייל-תחילה, עברית RTL,
`noindex`. **קרא את `README.md` לפני שינוי בקוד** — שם יושבות ההחלטות
הארכיטקטוניות והנימוקים שלהן.

המאגר הזה נחלץ מ-`yarin-debug/ganpereh-site` (שם הוא ישב תחת `app/`)
ב-28/07/2026. ההיסטוריה נשמרה. הפונטים הועתקו פנימה — האפליקציה כבר לא
מסתמכת על שום קובץ מחוץ למאגר.

---

## הרצה ובדיקה

אין שלב build ואין תלויות. ES modules טהורים, נפתח משרת סטטי:

```bash
python3 -m http.server 8899 --bind 127.0.0.1
# האפליקציה:  http://127.0.0.1:8899/
# הבדיקות:    http://127.0.0.1:8899/dev/tests.html
```

`dev/tests.html` הוא הרנס היחיד — נפתח בדפדפן ומציג סיכום ירוק/אדום.
**152 בדיקות עוברות כרגע.** הן רצות מול מפתח localStorage ייעודי
(`gp_meals_test__do_not_use`) ולעולם לא נוגעות בנתוני הייצור.

לבדיקה אוטומטית מלאה יש Chromium מותקן. **אין להריץ `playwright install`.**
הנתיב המדויק הוא עם מספר הגרסה — `chromium` לבדו אינו קיים:

```js
chromium.launch({ executablePath: "/opt/pw-browsers/chromium-1194/chrome-linux/chrome" });
```

מתקינים `playwright` בתיקיית סקראץ' (לא במאגר — אין כאן `package.json`
ואין תלויות), ומריצים מול השרת הסטטי המקומי.

**מלכודת בכתיבת בדיקות דפדפן:** השכבות נערמות (בורר מנה ← בורר מצרך ←
טופס), וכל המסכים נשארים ב-DOM גם כשהם מוסתרים. סלקטור גלובלי כמו
`.dish-card` יתפוס אלמנטים משכבות אחרות וממסכים מוסתרים. יש למקד תמיד:
`page.locator(".sheet").last()` לשכבה העליונה, `#screen-list` למסך.

## פרמוט

```bash
npx prettier@3 --write "**/*.{css,js}"
```

`.prettierignore` מחריג HTML בכוונה — `index.html` מלא בהערות עברית
במקומות רגישים, ופרמוט אוטומטי הזיז אותן.

---

## ⚠️ מלכודת פריסה

`sw.js` מחזיק `VERSION` (כרגע `v9`). **כל פריסה שמשנה קובץ ברשימת
`SHELL` חייבת להעלות אותו**, אחרת משתמשים ימשיכו לקבל את הגרסה הישנה
מהמטמון. הוספת קובץ JS חדש? להוסיף אותו ל-`SHELL` *וגם* להעלות VERSION.

המטמון אטומי בכוונה (הכל או כלום) כדי למנוע "מטמון מעורב" — `index.html`
חדש לצד JS ישן.

**כל הנתיבים יחסיים בכוונה.** אין אף `/`-מוביל, `start_url` ו-`scope`
ב-manifest הם `"."`, וה-service worker נרשם כ-`register("sw.js")`. לכן
האפליקציה עובדת גם משורש דומיין וגם מתת-נתיב (`user.github.io/meal-planner/`)
בלי שינוי. **נתיב מוחלט אחד ישבור את הגרסה בתת-נתיב** ולא יראו את זה
בפיתוח המקומי, שרץ מהשורש.

---

## עבודת עיצוב — חובה

כל שינוי ויזואלי עובר דרך הסקילים האלה, לא "לפי תחושה". זה כולל מסך
חדש, שינוי פריסה, טיפוגרפיה, צבע, מצבי ריק, ואנימציה.

| שלב | מה מפעילים | מתי |
|---|---|---|
| **כיוון** | סקיל `frontend-design` | *לפני* שכותבים שורת CSS ראשונה למסך חדש או לשינוי בשפה הוויזואלית |
| **מיקרו-קופי** | `/design:ux-copy` | כשכותבים מצבי ריק, הודעות שגיאה, ותוויות כפתורים |
| **ביקורת** | `/design:critique` | כשיש מסך עובד, לפני קומיט |
| **נגישות** | `/design:accessibility` | על כל מסך חדש, ובכל שינוי בפקדים |

`/design:*` הן פקודות של הפלאגאין **`design`**.

**⚠️ הסקילים מעדנים בתוך השפה הקיימת, לא מתחילים מחדש.** לאפליקציה
כבר יש כיוון עיצובי מוכרע ומתועד ב-`README.md` תחת "עיצוב": פלטת אריח
המטבח (קובלט לזהות, חרס לפעולה), TelAviv Brutalist + Alef, ופס השבוע
כאלמנט החתום. `frontend-design` נועד לקבל החלטות *בתוך* המערכת הזו.
סבב שמחליף את הפלטה או את הפונטים בלי סיבה מפורשת הוא רגרסיה, לא שדרוג.

**הכלל שאסור לשבור:** מילוי מלא שמור לפעולה *אחת* שצריך לעשות עכשיו.
מה שהושלם מקבל קו וגוון רך. הכלל הזה נולד מתלונה אמיתית — גרסה שציירה
"בישלנו" כמלבן כהה נפסלה כי *ביצוע* נקרא כמשקל.

תמיד לאמת ויזואלית לפני קומיט — יש Chromium מותקן, וצילום מסך ב-390×844
תופס פריסות שבורות שהקוד לבדו לא מגלה.

---

## מוסכמות כתיבה

- **הערות בעברית.** ההערה מסבירה *למה*, לא *מה*. הקוד כבר אומר מה.
- **ההערות שמתעדות החלטה נגד האינטואיציה — לא למחוק.** הן הסיבה שהחלטה
  נכונה לא מתהפכת בסיבוב הבא.
- אין תלויות חיצוניות ואין בקשות רשת חיצוניות. הפונטים מקומיים.
- כל מחרוזת שהמשתמש רואה היא בעברית.
- שינוי בלוגיקה טהורה (`normalize.js`, `plan.js`, `pantry.js`,
  `catalog.js`, `profiles.js`, `history.js`) מגיע עם בדיקה ב-`dev/tests.js`.

## Git

פיתוח על ענף ייעודי, לעולם לא ישירות על `main`.
הודעות קומיט בעברית, בגוף ההודעה מסבירים *למה* ולא רק *מה*.
**מיזוג ל-`main` הוא הפריסה** — GitHub Pages מגיש את השורש כמו שהוא.
