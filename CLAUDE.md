# CLAUDE.md — אתר גן פרא

## העלאת האתר לאוויר

כאשר ירין אומר "תעלה את האתר לאוויר" (או כל ניסוח דומה כמו "צא לאוויר", "דפלוי", "העלה את האתר"):

1. בצע `git add` לכל הקבצים שהשתנו
2. בצע `git commit` עם הודעה קצרה שמסכמת את השינויים (בעברית)
3. בצע `git push`
4. דווח רק על התוצאה — הצלחה או שגיאה

**אין לשאול שאלות. אין לבקש אישור. פשוט לבצע.**

תיקיית הפרויקט: `/Users/ashercohen/Desktop/קלוד/אתר קלוד`
ה-remote: `https://github.com/yarin-debug/ganpereh-site.git`

---

## מבנה האתר

- `index.html` — עמוד הבית
- `mirpeset.html`, `penthouse.html`, `asakim.html` — עמודי שירותים
- `blog.html` + קבצי `blog-*.html` — בלוג
- `quiz.html` — שאלון הצעת מחיר (טיוטה, לא מקושר מהאתר)
- `shared.css` — עיצוב משותף (נטען ע"י כל העמודים חוץ מ-index.html ו-quiz.html; index מכיל CSS אינליין משלו)
- `sitemap.xml`, `robots.txt` — SEO

---

## פרסום (GitHub Pages)

- האתר מתארח ב-**GitHub Pages** מ-branch `main`, דומיין מותאם `ganpereh.co.il` (CNAME מאומת, HTTPS פעיל). **push ל-main = פרסום אוטומטי.**
- הבנייה לעיתים **איטית** (עד כמה דקות). לאחר push כדאי לאמת מול האתר החי לפני שמדווחים "חי".
- אחרי פרסום, לראות שינויים בדפדפן דורש **רענון קשיח (Cmd+Shift+R)** — אחרת מוצג cache.
- `.gitignore` מוציא: `images-backup/`, `סרטונים/`, `.DS_Store`, `.claude-flow/`. לא להעלות אותם.

---

## מצב נוכחי — היכן עצרנו (עדכון: 26/06/2026)

### ⏸ פתוח להמשך — סשן הבא
**לבנות עמוד ארכיון פרויקטים — המשימה הבאה (ירין ביקש; דחה לסשן הבא).** עמוד ייעודי חדש שירכז את *כל* הפרויקטים, כל אחד עם תמונה + תיאור קצרצר; גלריית הבית (6 התמונות) היא "טעימה" שתוביל לעמוד הזה. **לפני שמתחילים — לסגור עם ירין:** (א) כמה פרויקטים וכמה תמונות לכל אחד; (ב) האם ירין כותב את התיאורים או שקלוד מנסח לפי פרטים שימסור (שם/מיקום/סוג גינה); (ג) חלוקה לפי קטגוריות (גינות/גגות/עסקים) או רשימה אחת. אפשר לשלב בארכיון את `binyan-*` ותמונות עסקים שלא נכנסו לגלריית הבית.

### עבודת 26/06/2026 — נגישות, ביצועים וליטוש (הכול **כבר באוויר**)
מקור: סקירת האתר מול הסקיל **web-design-guidelines** (Web Interface Guidelines של Vercel). שני commits: `aa0a129` (נגישות+ביצועים+ליטוש) ו-`848e6b1` (preload גופנים בעמודי משנה).

**נגישות:**
- **focus-visible** — טבעת פוקוס נראית למקלדת על כל לינק/כפתור (ירוקה; לבנה על רקע כהה). מוגדר ב-`shared.css` (כל העמודים) וגם אינליין ב-index. קודם לא היה כלום חוץ מ-quiz.
- **`scroll-padding-top: 84px`** (index + shared.css) — עוגנים (#contact וכו') לא נחבאים מתחת לתפריט הקבוע.
- **טופס יצירת קשר** (index): שגיאות מוטמעות ליד השדה + מיקוד אוטומטי על הראשון במקום `alert`; `role=status`/`aria-live` על הודעת ההצלחה; באנר שגיאת-רשת עם `role=alert`; `autocomplete="name"`/`"tel"`. הלוגיקה ב-JS (`setFieldError`/`clearFieldError`).

**ביצועים:**
- **preload לתמונת ה-hero** (`hero.jpg`, אלמנט ה-LCP) ב-index.
- **preload גופנים קריטיים** בכל העמודים: index ו-quiz = Brutalist-Bold + Alef-Regular; עמודי שירות + נחיתה (penthouse/mirpeset/asakim/landing-misradim) = Brutalist-Bold + Modernist-Regular. הגופנים שנבחרו = מה שבשימוש מעל הקפל בכל עמוד (אין preload מבוזבז — אומת בדפדפן ללא אזהרות).
- **`width`/`height` ל-12 תמונות התוכן** ב-index (שירותים/גלריה/בלוג) — מצמצם CLS. המידות אמיתיות (נמדדו ב-`sips`).

**ליטוש:**
- פעימת כפתור הוואטסאפ הצף (`.wa-float`) נעצרת ב-`prefers-reduced-motion`.
- **גלריית הבית — `cursor:pointer` הוסר** מ-`.gallery-item`: התמונות נראו לחיצות אבל אין lightbox/קישור (אפקט מטעה). כרגע רק זום-בריחוף. *אופציה עתידית: lightbox אמיתי.*
- `…` במקום `...` (index + quiz: select, placeholder, "שולח…", כותרת טעינת quiz).
- `safe-area-inset-bottom` לבר הוואטסאפ הדביק בנייד (index + shared) — לא נחתך מתחת ל-home-indicator באייפון.
- `touch-action: manipulation` על ה-body (index + shared) — מבטל עיכוב לחיצה בנייד.

**עסקי / SEO:**
- קופירייט בפוטר: 2025 → **2026**.
- **איחוד טלפון ב-Schema.org** ל-`054-552-5124` (היה המספר של ירין, 054-334-1118) ב-index/penthouse/mirpeset/asakim — תואם כעת למספר המוצג בכל האתר (NAP אחיד ל-SEO מקומי).

---

### עבודת 25/06/2026 — דף הבית והבלוג (הכול **כבר באוויר**)
- **גלריית הפרויקטים בדף הבית** — צומצמה ל-6 תמונות (3 גינות + 3 גגות); הוסרו 2 תמונות שבורות (`binyan-shtila.jpg`, `binyan-kordilina.jpg` — לא הועלו ל-git, נשארו מקומית ב-`images/gallery/` ולא בשימוש) וטאב הסינון "עסקים ומשרדים". נשארו 3 טאבים.

1. **תמונות תואמות-תוכן לכל המאמרים** — נוצרה תיקייה `images/blog/` עם תמונה ייעודית לכל מאמר (שם הקובץ = ה-slug של המאמר, למשל `klei-gina-tihzuka.jpg`). אותו קובץ משמש בשלושה מקומות: כרטיס דף הבית, כרטיס `blog.html`, וגוף המאמר — מקור אחד לכל מאמר, החלפה עתידית = קובץ אחד. תמונות שהוחלפו נשמרו ב-`images/blog/_unused/` (ב-gitignore).
2. **תמונה ראשית בגוף כל מאמר** — נוספה `<img class="article-hero-img">` בראש ה-`article-wrap` בכל 9 קבצי `blog-*.html`. הסגנון מוגדר פעם אחת ב-`shared.css` (`.article-hero-img` — רוחב מלא, גובה clamp, border-radius).
3. **דף הבית — צמצום המאמרים** — סקשן `#blog` ב-index מציג כעת 3 כרטיסים בלבד (featured + 2); כפתור "לכל המאמרים" מוביל ל-`blog.html` עם כל ה-9. **להוספה/הסרה של כרטיס בבית = עריכת `<section id="blog">` בלבד; `blog.html` מנוהל בנפרד.**
4. **תיקון משפט ההירו** — `.hero-sub` ב-index שונה מ"מאות פרויקטים בגוש דן" ל"עשרות פרויקטים בגוש דן" (דיוק ואמינות משפטית).
5. **ניקוי אבטחה** — הוסר PDF חשבונית מתיקיית "לוגואים של לקוחות" (היה tracked ב-repo הציבורי). הוסר מ-HEAD בלבד — עדיין בהיסטוריית git.

### עבודה קודמת (הכול **כבר באוויר**)
1. **מספרי טלפון** — הוחלף המספר של ירין במספר של עידו (054-552-5124) בכל קישורי החיוג/וואטסאפ/טקסט.
2. **תיקון טיפוגרפיה** בכרטיס הבלוג המוצג בדף הבית.
3. **הירו חדש** בדף הבית (פרטים למטה) — החליף גרסת "אורורה" ושורת תמונות רצות שנגנזו.
4. **כפתור חץ אחיד** הוחל על כל ה-CTA-ים באתר (פרטים למטה).
5. **תמונת הירו** — `images/hero.jpg` (אדריכלות + גינה) הוטמעה.

### ההירו (index.html, `<section id="hero">`)
- מבנה: `#hero-bg` (תמונת רקע) + `#hero-overlay` (שכבת כהות) + `#hero-lines` (קווי רשת אנכיים) + `#hero-inner` (kicker, `.hero-h1`, `.hero-sub`, כפתור חץ). הכול ב-CSS אינליין ב-index.
- **תמונה:** `images/hero.jpg` (1900×2850, ~900KB, נגזרה מ-`images/תמונה להירו.jpg`). הקאדר: `background-position: center 62%`. **להחלפת תמונה = שינוי ה-url וה-position בשורת `#hero-bg` בלבד.**
- אנימציות כניסה ב-CSS בלבד (אין JS להירו).

### מערכת כפתור החץ (`.btn-arrow`)
- מוגדר ב-**`shared.css`** (לכל העמודים) וגם אינליין ב-**index.html**.
- מבנה HTML: `<a class="btn-arrow"><span class="btn-arrow-label">טקסט</span><span class="btn-arrow-icon"><svg class="a1">…</svg><svg class="a2">…</svg></span></a>` (החץ הוא lucide arrow-up-left; שני עותקים לאנימציית ההחלקה בריחוף).
- ערכת צבעים דרך משתני CSS. **וריאנטים:**
  - ברירת מחדל = פיל קרם → ירוק בריחוף (לרקע **כהה**, למשל ההירו וסקשני CTA).
  - `.on-light` = פיל ירוק → טרקוטה בריחוף (לרקע **בהיר**).
  - `.btn-arrow-sm` = גרסה קומפקטית (ה-CTA בתפריט); מתחת ל-420px העיגול מוסתר ונשאר pill בלבד.
- **כפתורים שהושארו בכוונה ללא שינוי:** בר וואטסאפ דביק (`.sticky-wa-bar`), כפתור צף (`.wa-float`), שליחת טופס (`.btn-submit`), טאבי סינון הגלריה, חיצי כרטיסי השירות, ו-quiz.html. כפתורי הטלפון המשניים נשארו pill נקי (`.btn-outline`).

### פתוח / לעשות בהמשך
- ירין יבחר אולי תמונת הירו אחרת — אם כן, להחליף ב-`#hero-bg` ולכוונן קאדר.
- **ניקוי אופציונלי:** קבצים לא-מנוטרים ולא-בשימוש שאפשר למחוק מהפרויקט — `images/תמונה להירו.jpg` (מקור כבד 2.7MB) ו-`images/ChatGPT Image May 31...png` (placeholder ישן).
- **lightbox לגלריית הבית** (אופציונלי) — אחרי שהוסר `cursor:pointer` ב-26/06, התמונות לא לחיצות. שדרוג עתידי: הגדלה בלחיצה.
- **שינויים מקומיים שלא הועלו** (בכוונה, ירין ביקש להשאיר): מחיקת `לוגואים של לקוחות/ פלאנט ניין לוגו.svg` ותמונות `images/gallery/binyan-*.jpg` שאינן ב-git.
