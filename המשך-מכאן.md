# המשך מכאן — ליטוש עיצוב סבב ב'

> נכתב 19.8.2026 בסוף סשן 5. **סשנים 1-5 מוזגו ובאוויר** (`main` = `fd783f3`).
> התוכנית המלאה: `docs/תוכנית-ליטוש-עיצוב-סבב-ב.md`.
> כשהכל ייסגר — למחוק את הקובץ הזה.

---

## מה נשאר — לפי סדר

### 🔶 1. שתי הכרעות שממתינות לך (5 דקות, בלי לגעת בקוד)

**א. ה-morph בארכיון — האם לעבור ל-FLIP?**
התוכנית ביקשה; מדדתי ולא ביצעתי. הסיבה במספרים: יחס הכרטיסים בארכיון לא אחיד — 6 כרטיסים רגילים ביחס 1.24 ו-6 כרטיסי `span2` ביחס **2.54**, מול יעד 1.34. ‏FLIP נאיבי מותח את התמונה ב-8% בכרטיסים הרגילים וב-**47%** בכרטיסי ה-span2, כלומר חצי מהארכיון נמעך באמצע האנימציה.

| אפשרות | מה זה אומר |
|---|---|
| **א׳ · להשאיר** | ה-morph רץ על `position: fixed` עם תת-עץ זעיר. החשד לג'אנק הוא בטלפון, ולא הצלחתי למדוד (הדפדפן בסביבת העבודה מקפיא rAF בטאב מוסתר). **אם זה מרגיש חלק בטלפון שלך — זו התשובה.** |
| ב׳ · FLIP עם counter-scale | לא מותח, אבל משנה את אופי המעבר מ"חיתוך מחדש" ל"חשיפה", ודורש שינוי מבנה + בנייה מחדש של 13 עמודים |
| ג׳ · לאחד את יחסי הכרטיסים | למשל `span2` בגובה כפול. אז FLIP נאיבי מספיק, והגריד גם ייראה מסודר יותר |

**הדבר הכי שימושי שתעשה:** תפתח את `ganpereh.co.il/projects.html` בטלפון, תלחץ על כרטיס רחב, ותגיד אם המעבר מרגיש חלק.

**ב. שמירת המקום לשגיאות בטופס השאלון (סשן 3)**
התיקון מוסיף **63px** לגובה טופס הקשר (3 שדות × ~21px) — כפתור השליחה ירד מ-483px ל-546px. התמורה: הטופס לא זז בכלל כששגיאה נחשפת. אם זה מרגיש יקר, אפשר לצמצם.

---

### 🟡 2. סשן 6 — מסלול ה': קוהזיה ("החלפת צנרת", אפס שינוי ויזואלי)

היקף בינוני, אין תלות בך. מה שבפנים:
- **טאטוא טוקנים:** `index.css` מחזיק 40+ משכים ידניים; `projects.css/js` מקליד את אותה עקומה ×5; `lp.css` ו-`shared.css` מקלידים את ערך הטוקן עצמו.
- **מגירת המובייל** רצה על עקומת Material גנרית במקום `--ease-drawer` שהוגדר בדיוק בשבילה, וסגירה באותה מהירות כמו פתיחה.
- **אקורדיון המובייל** על `max-height: 0→620px` — אנימציית layout עם תקרה שרירותית. לעבור לתבנית ה-grid שכבר עובדת ב-FAQ.
- דרופדאון הניווט (יציאה מהירה יותר) · חץ ה-`.btn-arrow` מ-500ms ל-350 · זום תמונות אחיד · FAQ עם `aria-expanded` · observer משותף ב-`lp.js`.
- **אופציונלי, לסשן נפרד:** סולם טיפוגרפיה — 29 גדלי פונט שונים ב-`index.css` (הפרשים של 0.16px) ל-7 מדרגות.

⚠️ **לשים לב בסשן הזה:** שלוש פעמים בסבב הזה shorthand דרס longhand בשקט (סולם הסטגר ב-`lp.css`, דיליי החשיפה שדלף ל-`scale` בארכיון, ו-`animation-play-state` במרקי). טאטוא טוקנים נוגע בדיוק בקוד הזה — כדאי לאמת כל שינוי בערך המחושב בדפדפן, לא בקריאה.

---

### ✨ 3. סשן 7 — מסלול ו': רגעי שיא (לפי בחירה, קצר לכל פריט)

1. **מסך התוצאה של השאלון לא אומר "סיימת"** — אותה אנימציה כמו כל שאלה. הזול והחזק: להשאיר את פס ההתקדמות ולתת לו להתמלא לפני חשיפת הבלוקים.
2. **"זו האחרונה"** — רמז שקט לפני עמוד הפרטים, נקודת הנטישה הגבוהה ביותר.
3. **רינדור תוצאה אופטימי** — ⚠️ **דורש הכרעה שלך**, נוגע לשלמות צינור הלידים.
4. **המשכיות ארכיון→עמוד פרויקט** — `view-transition-name` ב-6 שורות.
5. **טופס לידים אינליין בדף הקמפיין** — ⚠️ **דורש הכרעה שלך**; היום שלושה קישורי וואטסאפ בלבד, ומי שאין לו וואטסאפ אין לו נתיב המרה.

---

## 🔎 ממצאים פתוחים שלא היו בתוכנית

| מה | חומרה | הערה |
|---|---|---|
| **פס ההתקדמות בשאלון מגיע ל-100% בשאלה הראשונה** ואז צונח ל-13% | בינונית | בשלב S1 קבוצת השלבים הנספרים מכילה רק את S1, כי המסלול עוד לא נבחר. קיים מלפני הסבב |
| **"חזרה" באמצע שליחה בשאלון** — ה-`await` ממשיך; הצלחה תקפיץ למסך התוצאה מאיפה שהמשתמש נמצא, ושליחה חוזרת תיצור ליד כפול | בינונית | קיים מלפני הסבב |
| **`.btn-ghost` הוא קוד מת** — אף עמוד לא משתמש בו | נמוכה | מועמד למחיקה בסשן 6 |

---

## 📌 הלקח שכדאי שיישאר

**תיקון ידני בעמודים שנוצרים מסקריפט מחזיק מעמד בדיוק עד הבנייה הבאה.**
הפיקסל של מטא הוחלף ידנית ב-37 עמודים ב-17.8, אבל `build_projects.py` — מקור האמת ל-13 עמודי הפרויקטים — נשאר עם הישן. ההרצה הראשונה שלו החזירה 13 עמודים לפיקסל המת. נתפס רק כי הסתכלתי בדיף.

מאז נסגר: שני סקריפטי הבנייה נסרקו (`build.py` נקי), אפס מופעים של הפיקסל הישן בריפו, ו-`about.html` — שהיה עם GA בלי פיקסל — קיבל אותו (36→37 עמודים).
