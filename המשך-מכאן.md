# המשך מכאן — תמונות האתר (30.8.2026)

## 🔶 ממתין לך — מיזוג

**[PR #50](https://github.com/yarin-debug/ganpereh-site/pull/50) פתוח ומוכן.**
כיוון הצילום קובע את הפריסה: לרוחב → כרטיס רחב והירו ברצועה ·
לאורך → כרטיס 3:4 והירו מפוצל (תמונה בצד, כותרת בצד).

**כבר באוויר:** תיקון המריחה בגלריות (`50102d8`) ו-[PR #49](https://github.com/yarin-debug/ganpereh-site/pull/49)
— תיקיית המקור, ההירו למובייל, וניקוי 81.7MB.

## התיקייה החדשה — מקור האמת לתמונות

`אתר קלוד/תמונות-האתר/פרויקטים/` — תיקייה לכל פרויקט, ובתוכה
`הירו · שער · גלריה · לפני-אחרי · ארכיון`. גוררים קבצים, מריצים
סקריפט, והאתר מתעדכן. ההוראות המלאות: `תמונות-האתר/README.md`.

התיקייה כבר מלאה ב-81 קובצי המקור המקוריים של מה שבאתר היום.

```bash
cd ~/Desktop/קלוד/"אתר קלוד" && python3 build_site_images.py --write && python3 build_projects.py && python3 build_assets.py --write
```

## 🔶 שלוש החלטות שנשארו לך

**1. כרטיס מודגש — רוצה אחד?** מעבר לצורה שנקבעת מהצילום, אפשר
להגדיל פרויקט לאריח 2×2. עולה כשלוש שורות גובה לעמוד. להוסיף slug
ל-`FEATURED` בראש `build_projects.py`.

**2. שישה פרויקטים מצולמים שאינם באתר** — בית פרטי בצהלה · חנות
מייקי ביהודה הלוי · מסעדה בשוק לווינסקי · משרדי אייקון · משרדי דרייב ·
משרדי ניין קלאוד. יש להם 84 צילומים בספריית הגלם ואין להם עמוד.

## 📌 לצילום הבא

**אתה כבר לא מוגבל לכיוון אחד** — האתר מקבל את שניהם ומציג כל אחד
לפי מה שמתאים לו. עדיין שווה לצלם כמה פריימים **לרוחב** בכל פרויקט:
הם מקבלים את הכרטיס הרחב בארכיון ואת ההירו הקולנועי, וזה מה שנותן
לעמוד הפרויקטים את הקצב שלו.

---

# המשך מכאן — ליטוש עיצוב סבב ב'

> עודכן 19.8.2026 בסוף סשן 7. **כל שבעת הסשנים מוזגו ובאוויר** (`main` = `423bd4d`).
> התוכנית המלאה: `docs/תוכנית-ליטוש-עיצוב-סבב-ב.md`.
> כשהכל ייסגר — למחוק את הקובץ הזה.

---

## 🔴 קודם כול — בדיקת עשן של 3 דקות (חובה)

סשן 7 שינה את **צינור הלידים החי** בשני מקומות. אימתי אותם מקומית במצב
יבש (`DRY_RUN`) בלבד — הדפדפן בסביבת העבודה חסום ב-CORS מול ה-API
האמיתי, ולכן שליחה אמיתית מקצה לקצה לא נבדקה. שתי בדיקות:

**1. השאלון** — לפתוח `ganpereh.co.il/quiz/`, למלא עד הסוף עם הפרטים שלך,
ולוודא ש**הליד הופיע בדשבורד**. מה שהשתנה: הפרופיל מוצג מיד, והשליחה
רצה ברקע. אם היא נכשלת — אמור להופיע באנר בראש מסך התוצאה עם כפתור
ניסיון חוזר וקישור וואטסאפ.

**2. דף הקמפיין** — לפתוח `ganpereh.co.il/landing-misradim.html`, לגלול
לתחתית, ולמלא את **הטופס החדש** (שם · טלפון · סוג חלל). לוודא שהליד
נכנס לדשבורד ושהמייל מ-Formspree הגיע.

אחרי שהשניים עברו — למחוק את שני לידי הבדיקה.

---

## 🔶 שתי הכרעות שממתינות לך (5 דקות, בלי לגעת בקוד)

**א. ה-morph בארכיון — האם לעבור ל-FLIP?**
נמדד ולא בוצע. יחס הכרטיסים בארכיון לא אחיד — 6 כרטיסים רגילים ביחס
1.24 ו-6 כרטיסי `span2` ביחס **2.54**, מול יעד 1.34. ‏FLIP נאיבי מותח
את התמונה ב-8% בכרטיסים הרגילים וב-**47%** בכרטיסי ה-span2.

| אפשרות | מה זה אומר |
|---|---|
| **א׳ · להשאיר** | ה-morph רץ על `position: fixed` עם תת-עץ זעיר. החשד לג'אנק הוא בטלפון, ולא הצלחתי למדוד. **אם זה מרגיש חלק בטלפון שלך — זו התשובה.** |
| ב׳ · FLIP עם counter-scale | לא מותח, אבל משנה את אופי המעבר מ"חיתוך מחדש" ל"חשיפה", ודורש שינוי מבנה + בנייה מחדש של 13 עמודים |
| ג׳ · לאחד את יחסי הכרטיסים | למשל `span2` בגובה כפול. אז FLIP נאיבי מספיק, והגריד גם ייראה מסודר יותר |

**הדבר הכי שימושי שתעשה:** לפתוח את `ganpereh.co.il/projects.html` בטלפון,
ללחוץ על כרטיס רחב, ולהגיד אם המעבר מרגיש חלק.
📌 בינתיים נוסף **מעבר חוצה-דפים** מהארכיון לעמוד הפרויקט (סשן 7) —
אותה תמונה ממשיכה משם לכאן. שווה לבדוק גם אותו באותה נסיעה.

**ב. שמירת המקום לשגיאות בטופס השאלון (סשן 3)**
התיקון מוסיף **63px** לגובה טופס הקשר. התמורה: הטופס לא זז בכלל
כששגיאה נחשפת. אם זה מרגיש יקר, אפשר לצמצם.

---

## 🟡 מה שנשאר מהתוכנית

**סולם הטיפוגרפיה** (מסלול ה', הסעיף האופציונלי) — נדחה בהחלטתך לסשן
עצמאי. ‏29 גדלי פונט שונים ב-`index.css` (הפרשים של 0.16px) ל-7 מדרגות
כטוקנים `--fs-1..7`. שינוי עדין-רוחבי; לבצע רק עם השוואת פיקסלים מלאה.

---

## ⚠️ סיכון שנשאר פתוח בשאלון

רענון דף אחרי **כשל** שליחה מאבד את השאלון: `submitted` נשמר אופטימית,
ולכן סשן שמור לא נטען מחדש. רשת ההצלה היא הבאנר על מסך התוצאה (ניסיון
חוזר + וואטסאפ עם כל האפיון בטקסט), ו-beacon שנשלח בעזיבת הדף אם
השליחה עוד באוויר. אם יתברר שזה קורה בפועל — התיקון הוא לשמור את
הכשל ב-sessionStorage ולהציע ניסיון חוזר גם אחרי רענון.

---

## ✅ מה נסגר בסבב הזה

שבעה סשנים, שבעה PR-ים מוזגים: תיקוני בזק · דף הקמפיין הממומן · השאלון
(שני סשנים) · ארכיון הפרויקטים · עמוד הבית · קוהזיה רוחבית · רגעי שיא.

שלושת הממצאים הפתוחים שנרשמו כאן אחרי סשן 5 — **כולם נסגרו בסשן 7**:
פס ההתקדמות שהגיע ל-100% בשאלה הראשונה · "חזרה" באמצע שליחה שיצרה ליד
כפול · `.btn-ghost` שהיה קוד מת (נמחק בסשן 6).

---

## 📌 הלקחים מהסבב

**תיקון ידני בעמודים שנוצרים מסקריפט מחזיק מעמד בדיוק עד הבנייה הבאה.**
הפיקסל של מטא הוחלף ידנית ב-37 עמודים, אבל `build_projects.py` נשאר עם
הישן. ההרצה הראשונה שלו החזירה 13 עמודים לפיקסל המת.

**‏CSS שנכתב בראש LTR נשבר בשקט ב-RTL.** ‏`left: -9999px` על ה-honeypot
בדף הקמפיין בורח לכיוון הגלילה ומרחיב את המסמך ל-10,350px. בעין לא רואים
כלום; נתפס רק במדידת `scrollWidth`.

**‏shorthand דורס longhand בלי שגיאה.** קרה שלוש פעמים בסבב — סולם
הסטגר ב-`lp.css`, דיליי החשיפה שדלף ל-`scale` בארכיון,
ו-`animation-play-state` במרקי. לאמת בערך המחושב בדפדפן, לא בקריאה.

---

# מסלול שני — ביצועים (20.8.2026)

> ✅ **[PR #48](https://github.com/yarin-debug/ganpereh-site/pull/48) מוזג ונפרס 20.8.2026** — `main` = `eac891d`. אומת על האתר החי.
> **דסקטופ: 100 בכל ארבע הקטגוריות** (ביצועים · נגישות · best-practices · SEO), LCP 0.75 שניות.
> **נייד: נגישות 100 · best-practices 100 · SEO 100 · ביצועים — חציון 74 מארבע ריצות**, LCP 4.45 שניות (היה 9.6). אין מיגרציה, אין תלות בך.
> נייד 66 → 76-79 · דסקטופ 91 → 96 · נגישות 97 → **100** · משקל הדף 5,142 → ~1,030 KiB · LCP נייד 11.8 → 5.2 שניות.
> תוקנו סעיפים 01, 03, 04, 05, 06, 07 מדוח האבחון. הפירוט המלא בגוף ה-PR.

## ✅ שתי ההכרעות — סגורות

**א. סעיף 02 — המטמון. ✅ הוכרע 20.8.2026: לא עכשיו.**
הרווח התכווץ אחרי שהדף ירד מ-5 מגה למגה. נשאר פתוח לחזור אליו. הרקע:
GitHub Pages כופה `Cache-Control: max-age=600` ואין קובץ תצורה ששובר את זה —
לא `_headers` ולא `.htaccess`. המשמעות: גולש שחוזר אחרי רבע שעה מוריד הכל מחדש.
שתי דרכים, שתיהן בלי לגעת בקוד:

1. **Cloudflare מול GitHub Pages** (חינם) — מעבירים DNS, מגדירים Cache Rule
   עם Edge TTL של שנה ל-`/videos/*`, `/images/*`, `/fonts/*`. בונוס: שרתי
   קצה קרובים יותר לגולש הישראלי.
2. **Netlify או Vercel** — קוראים כותרות מקובץ בריפו, ומתחברים לאותו
   מאגר GitHub שכבר קיים.

⚠️ אם עוברים ל-TTL של שנה — כל נכס שמתעדכן חייב **שם חדש**
(`hero-2026-08.webp`, לא `hero.webp`), אחרת גולשים חוזרים יראו את הישן.

**ב. ✅ favicon — נסגר 20.8.2026. best-practices 96 → 100.**
ירין העביר את סימן המריצה. **נמדד שהירוק שלו כמעט נעלם על לשונית כהה בכרום**, ולכן הוא
יושב על אריח ירוק כהה (`#1b3024`) עם הסימן בקרם — בחירתו מתוך שלוש אפשרויות.
המאסטר בכספת: `01-Brand/נכסים/` עם README. ⚠️ `apple-touch-icon.png` הוא ריבוע מלא
בלי פינות מעוגלות ובלי שקיפות — iOS מעגל בעצמו, ועיגול כפול נותן שוליים שחורים.
אין אף `<link rel="icon">` ואין קובץ. כל דפדפן מבקש `/favicon.ico` ומקבל
404 — בכל עמוד, לכל גולש. זה הכשל היחיד שנשאר ב-best-practices (96 במקום 100).
צריך ממך את הסימן: אות, מונוגרמה, או סמל. אחרי שתגיד — זו עבודה של רבע שעה.

## 📌 מה שנמצא ולא היה בדוח

- **ה-preload של הפונטים היה הפוך.** העמוד טען מראש את `Alef_Regular` —
  הפונט ה*שני* ב-`font-family`, כלומר נפילה, שמרונדר על 18 אלמנטים —
  בזמן ש-`TelAviv-ModernistRegular`, שצובע 338 אלמנטים, לא נטען מראש כלל.
- **הדוח מצא שתי שורות ניגודיות בפוטר. בפועל נכשלו שבע**, ועוד שתיים
  בכרטיסי הבלוג שנראות רק בדסקטופ (כי `#blog` מוסתר בנייד).
- **הצעת הדוח לדחות את פיקסל פייסבוק במלואו הייתה מפילה לידים.**
  `track.js` יורה `fbq("track","Lead")` מאחורי `if (window.fbq)`. בלי
  ה-shim המקומי, כל ליד שנסגר לפני שהסקריפט הגיע היה נופל בשקט — דווקא
  ההמרה שהקמפיין מייעל אליה. לכן ה-shim והתור נשארו אינליין, ורק
  הסקריפט הכבד נדחה.
- 🔴 **הראיה החזקה ביותר לאן ללכת הלאה — פיזור המדידות על האתר החי.**
  ארבע ריצות נייד רצופות החזירו **74 · 98 · 72 · 75**. הריצה שקפצה ל-98
  היא בדיוק זו שבה TBT היה 56ms במקום ~270. כלומר האתר *מסוגל* ל-98,
  והציון נקבע כמעט לגמרי לפי איך שסקריפטי הצד-השלישי נוחתים באותה ריצה.
  זה לא רעש שכדאי למצע — זו המפה: **כל מה שנשאר יושב במעבד הראשי.**
- **הבעיה שנשארה היא המעבד, לא הרשת.** אחרי כל התיקונים ה-LCP עומד על
  ~5.2 שניות עם השהיית רינדור של 4.7. אימות: הסרתי את כל שבעת הפונטים —
  ההשהיה לא זזה. זו עבודת מעבד ראשי (1.3 שניות נמדד, ×4 בהאטת לייטהאוס),
  ורובה GTM, הפיקסל, והסקריפט האינליין של העמוד. **הדוח כתב "ה-JavaScript
  קליל, לא לגעת" — בהאטה אמיתית מדדתי TBT של 620ms.** זה המסלול הבא אם
  רוצים לעבור 85.
- **בהאטה אמיתית** (`--throttling-method=devtools`) התמונה טובה בהרבה
  מהסימולציה: LCP **2.1 שניות**, ציון **82**. הסימולציה של לייטהאוס בוחרת
  את סרגל הוואטסאפ הקבוע כאלמנט ה-LCP; מחיקת הסרגל מעבירה את אותה השהיה
  בדיוק לאלמנט הבא, כלומר זה לא ספציפי לו.

## ⚠️ מלכודת שנתפסה — לזכור

גרסה ראשונה של `build_assets.py` כתבה את החבילות ל-`assets/`. כל `url()`
יחסי בתוך ה-CSS (15 מופעים — פונטים ותמונות רקע) נפתר יחסית ל*קובץ ה-CSS*,
ולכן חזר 404. **הפריסה לא זזה, אף בדיקת סגנון מחושב לא צעקה, ובדיקת
ה-diff החזירה אפס הפרשים** — כי שני הצדדים סבלו מאותו באג. התסמין היחיד
היה טקסט בפונט נפילה. החבילות יושבות בשורש, וזה מתועד בסקריפט.

## 🔧 שני סקריפטי בנייה חדשים

- `build_assets.py --write` — בונה את ארבע חבילות ה-CSS ומפנה אליהן את
  העמודים. **חובה להריץ אחרי כל שינוי ב-`shared.css`/`index.css`/`projects.css`/`lp.css`**,
  אחרת האתר יגיש את הגרסה הישנה. דורש `npm install` פעם אחת.
- `build_images.py --write` — מייצר את גרסאות 400/720 לתמונות כרטיסי השירותים.

`npm run build` מריץ את `build.py` ואת `build_assets.py` יחד.
